export class Ownership {
  constructor(id, userId, playerId, acquisitionDate) {
    this.id = id;
    this.userId = userId;
    this.playerId = playerId;
    this.acquisitionDate = acquisitionDate;
  }
}
